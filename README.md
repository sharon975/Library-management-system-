# Library-management-system-
A simple Library Management System implemented as a final semester project. It contains a database with at least three tables (Books, Members, and Borrow Records), each preloaded with sample data. The system demonstrates basic database operations such as creating, inserting, updating, and querying records.
